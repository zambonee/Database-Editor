<Window x:Class="DatabaseEditorV3.PreviewImportWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DatabaseEditorV3"
        mc:Ignorable="d"
        Title="Preview Imported Data" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="MinWidth" Value="70"/>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Margin" Value="3"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding CollectionPreview}"
                  AutoGenerateColumns="True"
                  AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                  AutoGeneratedColumns="DataGrid_AutoGeneratedColumns"
                  SelectionUnit="CellOrRowHeader"
                  AlternationCount="2"
                  AlternatingRowBackground="LightGray">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="ToolTip" Value="{Binding ErrorMessage}"/>
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
        <CheckBox Grid.Row="1" Content="Update existing rows" 
                  IsChecked="{Binding UpdateOnExists, Mode=OneWayToSource}"
                  IsEnabled="{Binding HasPrimaryKey}"
                  ToolTip="Update instead of inserting a row when the Primary Key value here matches the value in the database. Rows without a Primary Key value are inserted as new rows."/>
        <CheckBox Grid.Row="2" Content="Create new parent records" 
                  IsChecked="{Binding InsertOnNotExists, Mode=OneWayToSource}"
                  IsEnabled="{Binding HasReferencedTable}"
                  ToolTip="Create a new record in the parent table when no match can be made given the parent table column values here."/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Test" Click="Test_Click"/>
            <Button Grid.Column="1" Content="Save" Click="Save_Click"/>
            <Button Grid.Column="3" Content="Close" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
